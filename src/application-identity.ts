/** User-facing project name. */
export const APPLICATION_NAME = "eXpress MCP connector";

/**
 * Stable identifier for credential, lock, and state paths. Changing it would
 * orphan paired-device credentials and weaken cross-version send idempotency.
 */
export const APPLICATION_STORAGE_ID = "express-bot-msg";
